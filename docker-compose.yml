services:
  api:
    build: .
    container_name: ml-recommender-api
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app/app
      - ./ui:/app/ui
      - ./data:/app/data:ro
      - ./models:/app/models:ro
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
